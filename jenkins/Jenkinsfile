pipeline {
    agent none
    stages {
        stage('Install') {
            agent {
                docker { image 'node:20.15.0-alpine3.20' }
            }
            steps {
                sh 'npm ci'
                sh 'npm run build'
            }
        }
    }
}